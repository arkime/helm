{{- if .Values.cont3xt.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "arkime.fullname" . }}-cont3xt
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arkime.labels" . | nindent 4 }}
    app.kubernetes.io/component: cont3xt
  {{- with .Values.cont3xt.additionalAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.cont3xt.replicas }}
  {{- with .Values.cont3xt.revisionHistoryLimit }}
  revisionHistoryLimit: {{ . }}
  {{- end }}
  {{- with .Values.cont3xt.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "arkime.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: cont3xt
  template:
    metadata:
      labels:
        {{- include "arkime.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: cont3xt
        {{- with .Values.cont3xt.additionalLabels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with .Values.cont3xt.additionalAnnotations }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "arkime.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.cont3xt.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      {{- with .Values.cont3xt.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ . }}
      {{- end }}
      {{- with .Values.cont3xt.dnsPolicy }}
      dnsPolicy: {{ . }}
      {{- end }}
      {{- with .Values.cont3xt.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8}}
      {{- end }}
      {{- if .Values.cont3xt.hostNetwork }}
      hostNetwork: true
      {{- end }}
      {{- with .Values.cont3xt.schedulerName }}
      schedulerName: {{ . | quote }}
      {{- end }}
      {{- with .Values.cont3xt.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: arkime-cont3xt
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        command:
          {{- toYaml .Values.cont3xt.command | nindent 10 }}
        env:
          - name: POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
          - name: NODE_NAME
            valueFrom:
              fieldRef:
                fieldPath: spec.nodeName
          {{- range .Values.cont3xt.env }}
          - name: {{ .name }}
            value: {{ .value | quote }}
          {{- end }}
          # Ensure ARKIME_cont3xt__webBasePath is set from the specific value
          - name: ARKIME_cont3xt__webBasePath
            value: {{ .Values.cont3xt.webBasePath | quote }}
          {{- if .Values.env }} # Global envs
            {{- range .Values.env }}
            {{- if ne .name "ARKIME_cont3xt__webBasePath" }} # Avoid duplicating if present in global
          - name: {{ .name }}
            value: {{ .value | quote }}
            {{- end }}
            {{- end }}
          {{- end }}
        {{- with .Values.cont3xt.envFrom }}
        envFrom:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        volumeMounts:
          - name: arkime-config
            mountPath: /opt/arkime/etc/
          {{- if .Values.cont3xt.extraVolumeMounts }}
            {{- toYaml .Values.cont3xt.extraVolumeMounts | nindent 10 }}
          {{- end }}
        {{- with .Values.cont3xt.ports }}
        ports:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.cont3xt.resources }}
        resources:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        securityContext:
          runAsNonRoot: true
          # runAsUser: 1001 # OpenShift assigns
          # runAsGroup: 1001 # OpenShift assigns
          seccompProfile:
            type: RuntimeDefault
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
              - ALL
      volumes:
      - name: arkime-config
        configMap:
          name: {{ include "arkime.configMapName" . }} # Use helper
      {{- if .Values.cont3xt.extraVolumes }}
        {{- toYaml .Values.cont3xt.extraVolumes | nindent 6 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "arkime.fullname" . }}-cont3xt
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arkime.labels" . | nindent 4 }}
  {{- with .Values.cont3xt.additionalAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "arkime.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: cont3xt
  {{- with .Values.cont3xt.service.ports }}
  ports:
    {{- toYaml . | nindent 12 }}
  {{- end }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "arkime.fullname" . }}-cont3xt
  labels:
    {{- include "arkime.labels" . | nindent 4 }}
    app.kubernetes.io/component: cont3xt
spec:
  {{- with .Values.cont3xt.podDisruptionBudget.minAvailable }}
  minAvailable: {{ . }}
  {{- end }}
  {{- with .Values.cont3xt.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ . }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "arkime.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: cont3xt
{{- end }}